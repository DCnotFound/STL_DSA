#include<iostream>
#include <bits/stdc++.h>
using namespace std;

bool comp(pair<int, int> pl, paisint, int> p2) {
if(p1.second <p2.second) return true;
(p1.second > p2.second) return false;
//If they are sane
}
if(pl. first > p2.first) return true;
return false;
}

int main() {

sort(a, a + n);// (start,end) 
sort(v.begin(), v.end());
sort(a+2, a+4);
sort(a, a+n, greater<int>()); // sorting in decending order
pair<int, int> a[] = {{1,2}, {2, 1}, {4, 1}};
// sort it according to second element
// if second element is same, then sort
// it according to first element but in descending
sort(a, a+n,comp); // comp stands for a self written comparator which is a boolean function
// (4,1), (2, 1), (1, 2});
int num = 7;
int
cnt=_builtin_popcount();// finds out the binary of a number 
long long num= 165786578687;
int cnt=_builtin_popcountll();

// To print all the permuations of a string
string s = "123";
sort(s.begin(),sort.end());// sorting is done so that all the possible permutaions of the string is printed
do {
cout << s << endl;// prints the first the permutation i.e. the string itself i.e. 123
while(next_permutation (s.begin(), s.end()));// prints the next permuation and so on till the end
int maxi = *max_element(a,a+n);// tells us the maximum element 

return 0;
}